# Bug 修复报告 v1.0.1

## 修复日期
2026-06-10

## 修复的问题

### 1. ✅ 复活后的宠物不会攻击
**问题描述：**
- 战斗中死掉的宠物被治疗技能复活后，HP 恢复满血但不会攻击

**原因分析：**
- `BattleState.getCurrentPet()` 使用 while 循环向后查找活着的宠物
- 当宠物死亡时，索引已经跳过该宠物
- 治疗后宠物复活，但索引不会回头检查

**解决方案：**
- 修改 `getCurrentPet()` 函数逻辑
- 先从当前索引向后查找
- 如果没找到，从头开始循环查找（处理治疗/复活情况）

**修改文件：**
- `js/core/BattleState.js` (getCurrentPet 函数)

---

### 2. ✅ 手动战胜后当机无法正常游戏
**问题描述：**
- 手动模式战胜后，游戏会当机，无法继续操作

**原因分析：**
- `BattleState.clear()` 在 `setTimeout` 回调前被调用
- 结算界面需要访问战斗状态，但状态已被清空
- 导致后续操作失败

**解决方案：**
- 将 `BattleState.clear()` 移到 `closeResult()` 函数中
- 确保结算界面完全显示后才清理状态
- 添加 `isEndingBattle` 标志防止重复进入结算

**修改文件：**
- `js/ui/ManualBattle.js` (checkBattleEnd 和 closeResult 函数)

---

### 3. ✅ 战斗打赢后再回去打上一关，关卡会错乱
**问题描述：**
- 完成一关后，点击"◀"按钮回到上一关，关卡显示和实际战斗不匹配

**原因分析：**
- `changeStage()` 函数没有清理之前的战斗状态
- 旧的战斗结果、动画定时器等残留
- 导致状态混乱

**解决方案：**
- 在切换关卡前检查是否正在战斗
- 如果正在战斗，禁止切换并提示用户
- 清理所有战斗相关状态（timer, battleResult, logIndex）
- 调用 `BattleState.clear()` 确保状态干净

**修改文件：**
- `js/ui/BattleView.js` (changeStage 函数)

---

### 4. ✅ 打赢后结算画面增加"再打同一关"选项
**功能需求：**
- 战斗胜利后，除了"下一关"和"返回"，增加"再打一次"按钮

**实现方案：**
- 在胜利结算界面添加"再打一次"按钮
- 修改 `closeResult()` 函数，支持 `'retry'` 参数
- `'retry'` 模式下不改变 `currentStage`，直接重新渲染战斗界面
- 同时支持自动模式和手动模式

**修改文件：**
- `js/ui/BattleView.js` (finishBattle 和 closeResult 函数)
- `js/ui/ManualBattle.js` (checkBattleEnd 和 closeResult 函数)

**按钮布局：**
```
[下一关] [再打一次] [返回]
```

---

### 5. ✅ 打赢后有选择宠物文字瞬间LAG，导致按钮需要按2次
**问题描述：**
- 战斗胜利后，界面会显示"点击寵物可更換，準備好後出戰！"的文字
- 这个文字瞬间出现造成 LAG 感
- 用户点击"下一关"按钮时，第一次点击无效，需要按第二次

**原因分析：**
- 自动战斗模式有 2 秒自动跳转的定时器
- 定时器触发时会调用 `closeResult(true)`
- 如果用户在这 2 秒内点击按钮，会与自动跳转冲突
- 第一次点击被自动跳转"吃掉"，第二次才生效

**解决方案：**
- **移除所有自动跳转功能**
- 让用户主动选择下一步操作
- 自动模式和手动模式统一行为
- 避免定时器与用户操作的竞争条件

**修改文件：**
- `js/ui/BattleView.js` (finishBattle 函数，移除 setTimeout 自动跳转)
- `js/ui/ManualBattle.js` (checkBattleEnd 函数，移除 setTimeout 自动跳转)

---

## 测试建议

### 测试场景 1：复活技能测试
1. 选择有治疗技能的宠物（如森林系宠物）
2. 进入手动战斗
3. 让某个宠物 HP 降到接近 0
4. 使用治疗技能恢复 HP
5. 验证：被治疗的宠物在下一回合能正常攻击

### 测试场景 2：手动战斗完整流程
1. 选择手动模式
2. 完成一场战斗并获胜
3. 验证：结算界面正常显示，不会当机
4. 点击"再打一次"按钮
5. 验证：可以重新开始同一关战斗

### 测试场景 3：关卡切换
1. 完成一关战斗
2. 点击"下一关"进入下一关
3. 点击"◀"返回上一关
4. 验证：关卡显示正确，可以正常战斗

### 测试场景 4：按钮响应
1. 完成一场战斗（自动或手动）
2. 观察结算界面
3. 立即点击"下一关"或"再打一次"
4. 验证：按钮一次点击就能响应，不需要点击两次

---

## 技术改进点

### 代码质量提升
1. **状态管理更清晰**
   - 战斗状态的创建和清理时机更明确
   - 避免过早清理导致的空指针问题

2. **用户体验改善**
   - 移除自动跳转，给用户更多控制权
   - 增加"再打一次"选项，方便刷关

3. **防御性编程**
   - 添加 `isEndingBattle` 标志防止重复结算
   - 切换关卡前检查战斗状态

4. **循环查找逻辑**
   - 支持治疗后宠物重新进入战斗
   - 确保所有活着的宠物都能行动

---

## 版本信息

- **修复前版本：** v1.0.0 (Production Ready)
- **修复后版本：** v1.0.1
- **修改文件数：** 2 个文件
  - `js/core/BattleState.js`
  - `js/ui/ManualBattle.js`
  - `js/ui/BattleView.js`

---

## 后续建议

### 可选优化
1. 添加快捷键支持（如 Space 继续，R 重试）
2. 添加战斗速度记忆（保存用户选择的速度设置）
3. 添加"连续战斗"模式（自动进入下一关）
4. 优化手机端触摸响应

### 已知限制
- 纯前端实现，数据仅存于本地
- 无跨设备同步
- 无多人对战功能

---

**修复完成！所有问题已解决。** ✅
