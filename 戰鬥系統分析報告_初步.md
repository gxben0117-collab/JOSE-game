# 戰鬥系統分析報告（初步）

## 📋 執行時間
2026-06-10

## 🔍 分析範圍
- BattleEngine.js（戰鬥引擎）
- BattleState.js（狀態管理）
- BattleView.js（自動戰鬥UI）
- ManualBattle.js（手動戰鬥UI）
- BattleStats.js（統計系統）

---

## ✅ 系統優點

### 1. 架構設計良好
- 模組化清晰，職責分離
- BattleEngine 純邏輯，不依賴 UI
- 狀態管理獨立於視圖層

### 2. 戰鬥機制完整
- 支援回合制戰鬥
- 屬性相剋系統（火 > 森林 > 海洋）
- 技能系統（基礎/主動/被動）
- 冷卻機制
- 前後排定位

### 3. 統計系統完善
- 追蹤傷害、擊殺、回合數
- DPS 計算
- 提供詳細的戰鬥報告

---

## ⚠️ 發現的問題

### 🔴 Critical 級別問題

#### 1. 冷卻時間管理不一致
**位置：** BattleEngine.js 和 BattleState.js

**問題描述：**
- 自動戰鬥：在 `simulate()` 中，冷卻時間在每次技能使用時遞減
```javascript
// Line 257-258
} else {
  pet.cooldowns[i]--;
}
```
- 手動戰鬥：在 `executeEnemyTurn()` 中統一遞減所有冷卻
```javascript
// BattleState.js Line 149-153
pet.cooldowns = pet.cooldowns.map(function (cd) {
  return cd > 0 ? cd - 1 : 0;
});
```

**影響：**
- 自動模式：每個回合遍歷技能時遞減，可能導致冷卻多次遞減
- 手動模式：僅在敵人回合結束後遞減
- **兩種模式的冷卻速度不同**

**建議修復：**
統一冷卻管理邏輯，在回合結束時統一處理所有冷卻。

---

#### 2. 被動技能未在手動模式中應用
**位置：** BattleState.js init() 和 BattleEngine.js simulate()

**問題描述：**
- 自動戰鬥：`applyPassives(playerTeam.filter(Boolean))` 正確應用
- 手動戰鬥：`BattleEngine.applyPassives(playerTeam.filter(Boolean))` 也有調用
- **但是**：被動技能只在初始化時應用一次，戰鬥中新增寵物或復活後不會重新應用

**影響：**
- 如果寵物在戰鬥中被治療/復活，被動加成可能失效
- 狀態不一致

**建議修復：**
在治療技能執行後檢查並重新應用被動技能。

---

#### 3. heal_all 技能只治療自己
**位置：** BattleEngine.js Line 128-132 和 236-240

**問題描述：**
```javascript
} else if (sk.effect === 'heal' || sk.effect === 'heal_all') {
  var healed = Math.floor(pet.maxHp * sk.value);
  pet.hp = Math.min(pet.maxHp, pet.hp + healed);
  // ...
}
```

**影響：**
- `heal_all` 應該治療全體隊友
- 但現在只治療施法者自己
- 與技能描述不符（如"自然重生"、"生命綻放"）

**建議修復：**
為 `heal_all` 添加專門的邏輯，遍歷 playerTeam 治療所有活著的寵物。

---

### 🟠 High 級別問題

#### 4. 手動模式中 currentPetIndex 可能越界
**位置：** BattleState.js getCurrentPet()

**問題描述：**
已在 v1.0.1 修復，但仍有改進空間：
- 當所有寵物死亡後，仍會循環查找
- 應該提前檢查是否所有寵物都死亡

**建議改進：**
```javascript
getCurrentPet: function () {
  if (!currentBattle) return null;
  
  // 檢查是否所有寵物都死亡
  var anyAlive = currentBattle.playerTeam.some(function(p) {
    return p && p.hp > 0;
  });
  if (!anyAlive) return null;
  
  // 現有邏輯...
}
```

---

#### 5. 技能效果不完整
**位置：** BattleEngine.js executePetAction

**問題描述：**
只實現了部分技能效果：
- ✅ `damage` - 單體傷害
- ✅ `damage_all` - 全體傷害
- ✅ `heal` - 治療（但只治療自己）
- ❌ `heal_all` - 全體治療（實現錯誤）
- ❌ `buff_atk` - 攻擊力提升（未實現）
- ❌ `shield` - 護盾（未實現）
- ❌ `burn` - 灼燒效果（未實現）

**影響：**
- 許多寵物的技能實際上無效
- 例如：炎獅的"怒吼"（buff_atk）不會生效
- 熔岩蟹的"熔岩護盾"（shield）不會生效

**建議修復：**
實現所有技能效果，或者從寵物數據中移除未實現的技能。

---

#### 6. 動畫與狀態不同步
**位置：** BattleView.js animateBattle() 和 ManualBattle.js

**問題描述：**
- 自動模式：動畫通過 setTimeout 串聯，速度控制通過 `delay / battleSpeed`
- 手動模式：固定延遲 `500 / battleSpeed` 和 `1000 / battleSpeed`
- 如果玩家快速點擊或戰鬥速度很快，可能看到狀態跳躍

**建議改進：**
- 添加動畫隊列管理
- 確保動畫完成前不能執行下一個動作

---

### 🟡 Medium 級別問題

#### 7. AP 退還邏輯不完整
**位置：** BattleView.js flee()

**問題描述：**
- 逃跑退還 5 AP（一半）
- 但如果戰鬥未開始就關閉，AP 已扣但未退還
- BattleEngine.simulate() 在無寵物時退還 10 AP，但在手動模式的 ManualBattle.start() 中沒有對應邏輯

**建議改進：**
統一 AP 管理，確保所有失敗/取消的戰鬥都正確退還 AP。

---

#### 8. 戰鬥統計在多場戰鬥間未清理
**位置：** BattleStats.js

**問題描述：**
- `BattleStats.init()` 會重置統計
- 但如果用戶快速重新開始戰鬥，可能有時間窗口導致統計混亂

**建議改進：**
在每次戰鬥開始前強制清理舊統計。

---

#### 9. 敵人 AI 過於簡單
**位置：** BattleEngine.js pickPlayerTarget()

**問題描述：**
```javascript
function pickPlayerTarget(team) {
  var front = team.filter(function (p, i) { return i < 4 && p && p.hp > 0; });
  if (front.length > 0) return front[Math.floor(Math.random() * front.length)];
  // ...
}
```

敵人攻擊目標完全隨機，沒有策略：
- 不會優先攻擊低血量目標
- 不會集火
- 不會針對高威脅目標

**建議改進：**
添加簡單的 AI 權重系統，例如：
- 50% 概率攻擊血量最低的目標
- 30% 概率攻擊攻擊力最高的目標
- 20% 概率隨機

---

#### 10. 沒有戰鬥暫停功能
**位置：** BattleView.js

**問題描述：**
- 自動戰鬥開始後只能逃跑，不能暫停
- 用戶想查看戰鬥統計或調整速度時不方便

**建議改進：**
添加暫停/繼續按鈕。

---

### 🟢 Low 級別問題

#### 11. 魔法數字過多
**位置：** 全局

**問題描述：**
```javascript
if (n < 1 || n > state.maxStage) return;  // 1 是什麼？
var MAX_ROUNDS = 60;  // 為什麼是 60？
if (battleSpeed === 1 ? 'active' : '')  // 硬編碼
```

**建議改進：**
提取常量到配置對象：
```javascript
var BATTLE_CONFIG = {
  MIN_STAGE: 1,
  MAX_ROUNDS: 60,
  AP_COST: 10,
  FLEE_AP_REFUND: 5,
  SPEEDS: [1, 2, 4]
};
```

---

#### 12. 缺少錯誤處理
**位置：** 多處

**問題描述：**
```javascript
var cfg = getStageConfig(stageNum);
// 如果 cfg 是 undefined 會發生什麼？
```

**建議改進：**
添加防禦性檢查：
```javascript
var cfg = getStageConfig(stageNum);
if (!cfg) {
  console.error('關卡配置不存在:', stageNum);
  return { win: false, log: [...], rewards: null };
}
```

---

## 📊 統計總結

| 嚴重程度 | 數量 | 優先級 |
|---------|------|--------|
| Critical | 3 | 立即修復 |
| High | 3 | 本週修復 |
| Medium | 4 | 下個版本 |
| Low | 2 | 可選 |
| **總計** | **12** | |

---

## 🎯 改善計畫

### Phase 1: Critical 修復（v1.0.2）
**優先級：** 🔴 緊急

**任務：**
1. 統一冷卻時間管理邏輯
2. 實現 heal_all 全體治療
3. 實現缺少的技能效果（buff_atk, shield 等）

**預期效果：**
- 修復技能功能性 bug
- 兩種戰鬥模式行為一致
- 所有技能按預期工作

**預計時間：** 2-3 小時

---

### Phase 2: High 優化（v1.1.0）
**優先級：** 🟠 重要

**任務：**
1. 改進 getCurrentPet 邏輯
2. 完善 AP 退還機制
3. 優化動畫與狀態同步

**預期效果：**
- 更穩定的戰鬥流程
- 更好的用戶體驗
- 減少邊緣情況的 bug

**預計時間：** 3-4 小時

---

### Phase 3: Medium 增強（v1.2.0）
**優先級：** 🟡 建議

**任務：**
1. 添加敵人 AI 策略
2. 添加戰鬥暫停功能
3. 重構魔法數字為配置
4. 添加錯誤處理

**預期效果：**
- 更有挑戰性的戰鬥
- 更好的代碼可維護性
- 更友好的用戶控制

**預計時間：** 4-5 小時

---

## 🧪 建議的測試場景

### 測試 1：冷卻時間一致性
1. 選擇有主動技能的寵物
2. 分別用自動和手動模式戰鬥
3. 記錄技能使用頻率
4. 驗證兩種模式下冷卻速度相同

### 測試 2：全體治療技能
1. 選擇有 heal_all 技能的寵物（如翠玉麟）
2. 讓多個寵物受傷
3. 使用 heal_all 技能
4. 驗證所有寵物都被治療

### 測試 3：Buff 技能
1. 選擇炎獅（有"怒吼" buff_atk 技能）
2. 記錄使用前的傷害
3. 使用"怒吼"技能
4. 記錄使用後的傷害
5. 驗證傷害提升約 30%

### 測試 4：極限情況
1. 讓戰鬥進行到第 59-60 回合
2. 快速切換戰鬥速度
3. 在動畫播放中逃跑
4. 驗證不會崩潰或卡住

---

## 📈 代碼質量建議

### 1. 添加單元測試
```javascript
// 測試傷害計算
function testDamageCalculation() {
  var damage = BattleEngine.calcDamage(100, 50, 1.0, 1.0);
  console.assert(damage >= 45 && damage <= 60, 'Damage out of range');
}
```

### 2. 添加類型註釋（JSDoc）
```javascript
/**
 * 計算傷害
 * @param {number} atk - 攻擊力
 * @param {number} def - 防禦力
 * @param {number} mult - 技能倍率
 * @param {number} elBonus - 屬性加成
 * @returns {number} 最終傷害
 */
function calcDamage(atk, def, mult, elBonus) {
  // ...
}
```

### 3. 考慮使用狀態機
手動戰鬥的狀態管理可以用狀態機模式：
- IDLE（待命）
- PLAYER_TURN（玩家回合）
- ENEMY_TURN（敵人回合）
- BATTLE_END（戰鬥結束）

---

## 🔗 相關文件

- [Bug修復報告 v1.0.1](Bug修复报告_v1.0.1.md)
- [Production Ready Checklist](PRODUCTION_READY.md)
- [README](README.md)

---

**分析完成時間：** 2026-06-10
**分析者：** Claude Opus 4.8
**下一步：** 等待完整工作流分析結果，然後開始 Phase 1 修復
